package containers

import (
	"context"

	"github.com/briheet/nozarashi/internal/specs"
)

// Helps in creating images
// Switches on Node's service specs and send for required backends
func createImages(ctx context.Context, graph *specs.Graph) error {
	return nil
}

// Helps in creating Resources
func createResources(ctx context.Context, graph *specs.Graph) error {
	return nil
}
