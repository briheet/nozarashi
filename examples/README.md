# Examples

Input references:

- [`input_flake`](input_flake): package from a Nix flake
- [`input_git`](input_git): package from a Git repository
- [`input_path`](input_path): package from a local Nix file

Complete environments:

- [`simple_docker`](simple_docker): OCI services with a Dockerfile backend
- [`mix_docker_nix`](mix_docker_nix): Dockerfile backend with local Nix services
