# Git input

Build Redis by fetching and importing the `nixpkgs` Git repository at a
specific branch, tag or commit.

```sh
sudo ../../bin/nozarashi create
../../bin/nozarashi build
../../bin/nozarashi up
../../bin/nozarashi logs redis
../../bin/nozarashi destroy
```
