# Flake input

Build Redis directly from the `nixpkgs` flake.

```sh
sudo ../../bin/nozarashi create
../../bin/nozarashi build
../../bin/nozarashi up
../../bin/nozarashi logs redis
../../bin/nozarashi destroy
```
